<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.inatechno.javabasicapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JavaBasicApp">
        <activity android:name=".TangkapDataAlertActivity"></activity>
        <activity android:name=".DetailCustomGridActivity" />
        <activity android:name=".CustomGridViewActivity" />
        <activity android:name=".GridViewBiasaActivity" />
        <activity android:name=".DetailCustomListViewActivity" />
        <activity android:name=".CustomListViewActivity" />
        <activity android:name=".ListViewBiasaActivity" />
        <activity android:name=".VideoViewActivity" />
        <activity android:name=".CustomAlertActivity" />
        <activity android:name=".TabViewActivity" />
        <activity android:name=".SpinnerActivity" />
        <activity android:name=".RecyclerViewActivity" />
        <activity android:name=".GridViewActivity" />
        <activity android:name=".ListviewActivity" />
        <activity android:name=".WebviewActivity" />
        <activity
            android:name=".SplashScreenActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>